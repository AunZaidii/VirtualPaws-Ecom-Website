// Configuration file for client-side environment variables
// NOTE: Only put PUBLIC keys here (publishable keys, not secret keys!)
// Copy this file to config.js and add your actual keys

export const config = {
    stripe: {
        publishableKey: 'pk_test_your_stripe_publishable_key_here'
    }
};
