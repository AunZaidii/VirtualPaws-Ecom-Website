<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 600">
  <defs>
    <linearGradient id="g" x1="0" x2="1">
      <stop offset="0" stop-color="#e7b400" />
      <stop offset="1" stop-color="#f0c94a" />
    </linearGradient>
    <style>
      .title { font-family: 'Nunito', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial; font-weight:700; font-size:64px; fill:#fff }
      .subtitle { font-family: 'Nunito', sans-serif; font-size:24px; fill:#fff; opacity:0.95 }
      .cta { font-family: 'Nunito', sans-serif; font-weight:700; font-size:18px; }
    </style>
  </defs>
  <rect width="100%" height="100%" fill="url(#g)" />

  <!-- Stylized paw circle on right -->
  <g transform="translate(820,120) scale(1.2)">
    <circle cx="0" cy="0" r="110" fill="#fff" opacity="0.08" />
    <g fill="#fff">
      <circle cx="-30" cy="-40" r="18" />
      <circle cx="30" cy="-40" r="18" />
      <circle cx="-60" cy="-10" r="18" />
      <circle cx="60" cy="-10" r="18" />
      <path d="M0 12c-42 0-76 34-76 76v8h152v-8c0-42-34-76-76-76z" />
    </g>
  </g>

  <!-- Left illustration placeholder (rounded rectangle) -->
  <rect x="60" y="60" width="520" height="420" rx="24" ry="24" fill="#fff" opacity="0.12" />
  <rect x="120" y="120" width="400" height="300" rx="18" ry="18" fill="#fff" opacity="0.18" />

  <!-- Text block -->
  <g transform="translate(660,160)">
    <text class="title">Find your forever</text>
    <text class="title" y="72">Friend</text>
    <text class="subtitle" y="130">Browse loving pets waiting for a new home —</text>
    <text class="subtitle" y="160">from trusted shelters & NGOs</text>

    <!-- CTA button look (visual only, not interactive in SVG) -->
    <rect x="0" y="210" width="260" height="56" rx="36" fill="#fff" />
    <text class="cta" x="130" y="248" text-anchor="middle" fill="#2f6b25">See pets near you</text>
  </g>
</svg>